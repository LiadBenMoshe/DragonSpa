<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dragonspa">
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DragonSpa">
        <activity android:name=".ConnectUs"></activity>
        <activity android:name=".appointment_editing2" />
        <activity android:name=".Result" />
        <activity android:name=".ShowDateMassage" />
        <activity android:name=".product_adding2" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".appointments_editing" />
        <activity android:name=".product_adding" />
        <activity android:name=".manager_main_menu" />
        <activity android:name=".Manager_login" />
        <activity android:name=".register_activity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>